name: Default
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: export
      - run: cpanm App::mymeta_requires
      - run: perl Makefile.PL
      - run: mymeta-requires | cpanm
      - run: make
      - run: make test
      - run: make dist
